package v1alpha1

/*
// Data repository, e.g., S3
type DataRepository interface {
	GetType() string
	GetProvider() string
	GetRepoIdentifier() string
	// Get a structure for specific handler, e.g., for s3 it will return
	// a structure which has s3 specific info and API
	GetDataRepositoryHandlers() interface{}
}

// Describe the data from data repository, e.g., resitc snapshot ID
type DataDescriptor interface {
	DataRepository
	GetDataIdentifier() string
	GetDataDescriptorHandlers() interface{}
}

// Source of the data, e.g., PV
type DataSource interface {
	GetDataSourceType() string
	GetDataSourceReference() corev1.ObjectReference
	// Return a structure for specific handler, which includes more details
	// E.g., volumesnapshot, PV, database table are different data source
	GetDataSourceHandlers() interface{}
}
*/
